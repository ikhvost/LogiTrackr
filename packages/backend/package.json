{
  "name": "@saas-versioning/backend",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rimraf dist",
    "build": "pnpm clean && tsc",
    "watch": "tsx watch src/main.ts",
    "start": "node ./dist/main.js",
    "lint": "eslint tests src",
    "format:check": "prettier --check --ignore-unknown .",
    "format:fix": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml}\"",
    "test": "vitest run --poolOptions.threads.singleThread --silent"
  },
  "dependencies": {
    "@fastify/env": "^4.4.0",
    "ajv": "^8.16.0",
    "drizzle-orm": "0.33.0",
    "fastify": "^4.28.0",
    "inversify": "^6.0.2",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.3",
    "builder-pattern": "^2.2.0",
    "drizzle-kit": "^0.24.2",
    "tsx": "^4.15.6",
    "vitest": "^1.6.0"
  }
}
